streamlit>=1.28.0
scrapegraphai>=1.0.0
playwright>=1.40.0
# Additional dependencies for scrapegraphai
beautifulsoup4>=4.12.0
requests>=2.31.0
# Quick Wins Sprint Dependencies
tenacity>=8.2.0          # Retry logic with exponential backoff
pydantic>=2.0.0          # Schema validation (already in scrapegraphai)
python-dotenv>=1.0.0     # Environment variables
# Phase 2 Dependencies
redis>=5.0.0             # Caching system
textual>=0.47.0          # TUI framework (legacy Python TUI)
httpx>=0.25.0            # Async HTTP client for TUI
# Phase 3 Dependencies (v3.0.5 - TUIjoli Migration)
fastapi>=0.104.0         # REST API server
uvicorn[standard]>=0.24.0  # ASGI server with websocket support
python-multipart>=0.0.6  # Form data parsing
